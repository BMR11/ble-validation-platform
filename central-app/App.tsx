/**
 * BLE central demo — scan, connect, GATT with react-native-ble-manager.
 */

import React from 'react';
import CentralApp from './src/CentralApp';

function App(): React.JSX.Element {
  return <CentralApp />;
}

export default App;
