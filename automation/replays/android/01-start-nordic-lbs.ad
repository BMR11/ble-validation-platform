# Android: RN often omits testID from resource-id; use accessibility labels (see ProfileApp accessible={false} on children).
context platform=android
open "Peripheral App"
wait 2000
scroll down
wait 400
scroll down
wait 400
click "id=peripheral-profile-nordic-lbs"
wait 600
click "label=\"Start peripheral\""
wait 2000
